# apps

End-user applications on `app-01`: NetBox, WikiJS, and Nginx as the reverse
proxy, run as Podman Quadlets. These exist to give the identity and
observability layers something real to protect and measure. Arrives at M3.
