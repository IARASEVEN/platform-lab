# playbooks

One playbook per profile — `identity`, `observability`, `apps` — invoked
through the corresponding `make` target, never directly in the docs. Arrive
with their milestones, starting at M1.
